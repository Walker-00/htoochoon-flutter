name: htoochoon_flutter
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ">=3.8.0 <4.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  retrofit: ^4.9.2
  dio: ^5.9.0
  provider: ^6.1.5+1
  firebase_core: ^3.12.1
  firebase_auth: ^5.5.1
  firebase_messaging: ^15.1.3
  cloud_firestore: ^5.6.5
  shared_preferences: ^2.5.4
  google_sign_in: ^7.2.0
  google_fonts: ^7.1.0
  intl: ^0.20.2

  image_picker: ^1.2.1
  firebase_storage: ^12.4.10
  lottie: ^3.3.2
  foundation: ^0.0.5
  uuid: ^4.5.2
  json_annotation: ^4.11.0
  pin_code_fields: ^9.1.0
  otp_pin_field: ^1.4.1
  flutter_webrtc: ^1.3.1
  socket_io_client: ^3.1.4
  # Detached whiteboard opens in its own OS window on desktop (no-op on mobile,
  # which falls back to a full-screen route).
  # VENDORED local copy: linux/flutter_window.cc patched so closing a sub-window
  # (titlebar X / WM kill-active) HIDES instead of destroying its engine —
  # destroying it on Linux kills the whole process (main app + meeting).
  desktop_multi_window:
    path: third_party/desktop_multi_window
  permission_handler: ^12.0.1
  shimmer: ^3.0.0
  flutter_quill: ^11.5.1
  file_picker: ^8.0.0
  url_launcher: ^6.3.0
  flutter_html: ^3.0.0
  cached_network_image: ^3.4.1
  collection: ^1.19.1
  flutter_secure_storage: ^10.2.0
  mediasfu_mediasoup_client: ^0.1.3
  flutter_screen_recording: ^2.0.25
  vsc_quill_delta_to_html: ^1.0.5
  flutter_background: ^1.3.1
  markdown: ^7.3.1
  flutter_local_notifications: ^22.0.0

  # Exam proctoring (camera face/behavior + audio voice detection)
  camera: ^0.11.0
  google_mlkit_face_detection: ^0.13.1
  google_mlkit_object_detection: ^0.15.0
  noise_meter: ^5.0.2
  # Gyroscope/accelerometer for the pre-exam room scan: verifies the student
  # actually pans the phone around the room (not just waits out a countdown).
  sensors_plus: ^6.1.1

  # Analytics dashboards: bar/line charts for org-level reports.
  fl_chart: ^0.69.2

  # Chat voice notes: record audio + play it back.
  # record 7.x keeps all record_* implementations on a consistent ^2.x line
  # (5.x shipped a broken record_linux <1.0.0 cap that fails the build).
  record: ^7.1.0
  audioplayers: ^6.1.0
  path_provider: ^2.1.4
  # Session notes export/share + recording share, version gate, deep links, and
  # gallery save for local recordings/notes.
  share_plus: ^10.1.2
  package_info_plus: ^8.1.1
  app_links: ^6.3.2
  media_store_plus: ^0.1.3
  home_widget: ^0.9.3


dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.

  flutter_lints: ^5.0.0


  json_serializable: ^6.13.0
  retrofit_generator: ^10.2.4
  build_runner: ^2.13.1
  flutter_launcher_icons: "^0.14.4"

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/logos/main_logo.jpeg"
  min_sdk_android: 21 # android min sdk min:16, default 21
  web:
    generate: true
    image_path: "assets/images/logos/main_logo.jpeg"
    background_color: "#hexcode"
    theme_color: "#hexcode"
  windows:
    generate: true
    image_path: "assets/images/logos/main_logo.jpeg"
    icon_size: 48 # min:48, max:256, default: 48
  macos:
    generate: true
    image_path: "assets/images/logos/main_logo.jpeg"
# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Generate localizations (lib/l10n/*.arb -> AppLocalizations). See l10n.yaml.
  generate: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/lottie/
    - assets/lottie/STUDENT.json
    - assets/lottie/teach.json
    - assets/images/logos/
    - assets/images/logos/main_logo.jpeg
    - assets/images/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
